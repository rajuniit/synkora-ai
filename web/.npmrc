# Security settings to prevent compromised packages
# package-lock is enabled for CI frozen-lockfile support
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=false
# Only use registry from npmjs
registry=https://registry.npmjs.org/
